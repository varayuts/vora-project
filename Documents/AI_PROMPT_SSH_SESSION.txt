# 🤖 AI Assistant Prompt - MyAGV SSH Session

Copy this prompt when starting new SSH session to MyAGV:

---

## CONTEXT

I'm working on VORA Voice-Controlled Robot project with MyAGV.

**Current Setup:**
- MyAGV Jetson Nano (192.168.0.111) - Static IP ✅
- Gateway Windows (192.168.0.60) - Static IP ✅
- VORA Server (user.tail87d9fe.ts.net) - Tailscale

**Working Components:**
- ✅ ROSBridge: ws://192.168.0.111:9090
- ✅ Command Executor: ROS2 node listening on /vora/command
- ✅ Audio Stream: MyAGV → Gateway (ws://192.168.0.60:9001/gw/audio)
- ✅ Gateway → VORA Server: STT and command processing
- ✅ Voice commands detected (wake word: "โวร่า", "วูล่า", "ฮัลโหลล่า")

**Issue:**
- ❌ Robot receives /cmd_vel messages but DOESN'T MOVE
- ❌ MyAGV hardware driver (motor controller) missing or not configured

**System Architecture:**
```
Voice → MyAGV Mic → Gateway (192.168.0.60) → VORA Server (STT) → 
Gateway (Motion Parser) → ROSBridge → /cmd_vel → ❌ Motor Controller? → Robot
```

**Current ROS2 Workspace:**
- Path: `~/vora_ws/` (contains vora_robot_bridge)
- Path: `~/myagv_ros2/` (Elephant Robotics MyAGV ROS2 - incomplete build)

---

## GOAL

Help me set up MyAGV motor controller so robot responds to /cmd_vel messages.

---

## TASKS

1. **Diagnose:** Find why robot doesn't move when receiving /cmd_vel
2. **Identify:** What motor controller package/node is needed
3. **Fix:** Get robot to respond to voice commands
4. **Document:** Update start_myagv.sh with correct startup sequence

---

## AVAILABLE FILES

**Project Structure:**
```
~/Desktop/VORA_myAGV_only_ros2_package/new/
├── start_myagv.sh          # Automated startup (3 terminals)
├── send_audio_to_gateway.py # Audio streaming
├── vora_robot_bridge/       # ROS2 package
│   └── command_executor.py  # Receives JSON commands
└── README_MYAGV_ONLY.md     # Setup guide

~/vora_ws/
└── src/vora_robot_bridge/   # ROS2 workspace

~/myagv_ros2/
├── src/myagv_odometry/      # Has package but no launch file
├── src/myagv_description/   # URDF/visualization
└── src/myagv_navigation2/   # Nav2 integration
```

**Key Scripts:**
- `start_myagv.sh 192.168.0.60` - Start all services (updated without hardware driver)
- Gateway running on Windows with `start_gateway.sh`

---

## QUICK DIAGNOSTIC COMMANDS

```bash
# Check current state
ros2 topic list
ros2 node list
ros2 topic info /cmd_vel
ros2 topic echo /cmd_vel

# Test manual movement
ros2 topic pub --rate 10 /cmd_vel geometry_msgs/Twist \
  "{linear: {x: 0.1, y: 0.0, z: 0.0}, angular: {x: 0.0, y: 0.0, z: 0.0}}"

# Check serial devices (motor controller usually uses serial)
ls /dev/ttyUSB* /dev/ttyACM* 2>/dev/null

# Check myagv packages
ros2 pkg list | grep myagv
ros2 pkg executables myagv_odometry
```

---

## WHAT I NEED

1. Help identify MyAGV motor controller setup
2. Create/fix motor controller node if needed
3. Test robot movement with /cmd_vel
4. Update documentation and scripts

---

## CONSTRAINTS

- ROS2 Galactic on Ubuntu 20.04
- Jetson Nano (ARM architecture)
- Elephant Robotics MyAGV
- Already have: ROSBridge, Command Executor, Audio working
- Just need: Motor controller to make robot move

---

## SESSION FOCUS

Focus on practical fixes - we need robot to move when receiving /cmd_vel. 
Don't rebuild entire workspace unless necessary.
Prefer quick solutions that work with existing setup.

---

**Start here:** Run diagnostic commands above and help me identify the issue.
